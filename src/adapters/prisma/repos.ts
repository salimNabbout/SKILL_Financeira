// STUB — substituído pela implementação completa dos repositórios Prisma.
import type { Repositories } from "@/core/repositories";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function createPrismaRepositories(_prisma: any): Repositories {
  throw new Error(
    "Repositórios Prisma ainda não implementados neste build. Use DEMO_MODE=1 ou implemente adapters/prisma/repos.ts."
  );
}
